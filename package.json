{
  "name": "option-select-action",
  "description": "add an option object to a value array or call a fuction based on the object's properties",
  "version": "0.2.0",
  "repository": {
    "url": "nrw/option-select-action"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "tape test/*.js",
    "watch": "nodemon -q -x 'tape test/*.js' | tap-colorize"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "2.x.x"
  }
}
